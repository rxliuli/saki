{
  "name": "saki",
  "scripts": {
    "pub": "dotenv \"goreleaser release --rm-dist\"",
    "install-local": "go build && go install",
    "test": "go run main.go run setup --filter libs/*"
  },
  "devDependencies": {
    "dotenv-cli": "^4.1.1"
  }
}